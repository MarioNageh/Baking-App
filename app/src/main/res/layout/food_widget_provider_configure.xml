<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:id="@+id/text_configration_display"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:text="@string/selectionconfigration"
        android:textAlignment="center"
        android:textColor="@color/cardview_dark_background"
        android:textSize="20sp" />

    <android.support.v7.widget.RecyclerView
        android:layout_below="@id/text_configration_display"
        android:id="@+id/recylerview_mainfragment_config"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

    </android.support.v7.widget.RecyclerView>

    <ProgressBar
        android:id="@+id/pro_bar_config"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
       android:layout_centerInParent="true"
        android:visibility="gone"
         />
    <ImageView
        android:visibility="gone"
        android:id="@+id/no_internt_connection"
        android:layout_width="match_parent"
        android:src="@drawable/nointer"
        android:background="@color/material_color_white"
        android:layout_height="match_parent">
    </ImageView>

</RelativeLayout>